# 수능문항생성-검증-개선 시스템

> (origin: google appscript-mj)

---

## 📋 앱 정보

| 항목 | 내용 |
|------|------|
| **앱 명칭** | 수능문항생성-검증-개선 시스템 |
| **로컬 저장소** | `D:\csat_mj_generator` |
| **GitHub Repository** | https://github.com/smilepat/csat_mj_generator |
| **서버 주소** | http://localhost:3001 |
| **클라이언트 주소** | http://localhost:3000 |

---

## 🎯 앱 개요

### 목적
구글 앱스크립트 + 구글시트로 구성된 **"수능문항 프롬프트 - 생성 - 검증 - 재생성"** 시스템을 VS Code + Claude Code 환경에서 재구성한 개선용 앱입니다.

### 주요 기능
1. **문항 생성** - LLM(Gemini/OpenAI)을 활용한 수능 영어 문항 자동 생성
2. **문항 검증** - 생성된 문항의 형식 및 내용 유효성 검증
3. **재생성** - 검증 실패 시 자동 재시도 (최대 3회)
4. **프롬프트 관리** - 마스터 프롬프트 및 문항별 프롬프트 편집
5. **세트 문항** - 연계 문항(16-17, 41-42, 43-45) 세트 관리

---

## 🏗️ 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    사용자 브라우저                            │
│                  http://localhost:3000                       │
└─────────────────────┬───────────────────────────────────────┘
                      │ HTTP 요청
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                Express.js 서버 (Node.js)                     │
│                  http://localhost:3001                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ /api/    │  │ /api/    │  │ /api/    │  │ /api/    │    │
│  │ items    │  │ prompts  │  │ sets     │  │ config   │    │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘    │
└───────┼─────────────┼─────────────┼─────────────┼──────────┘
        │             │             │             │
        ▼             ▼             ▼             ▼
┌─────────────────────────────────────────────────────────────┐
│              itemPipeline (문항 생성 파이프라인)              │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐           │
│  │ 지문    │→│ 프롬프트│→│ LLM     │→│ 검증    │           │
│  │ 생성    │ │ 구성    │ │ 호출    │ │ 처리    │           │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘           │
└───────────────────────────┬─────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   Gemini     │    │   OpenAI     │    │   SQLite     │
│   API        │    │   API        │    │   Database   │
└──────────────┘    └──────────────┘    └──────────────┘
```

---

## 🔄 문항 생성 작동 흐름

### 1단계: 요청 생성
```
사용자 입력 → 요청 생성 API → item_requests 테이블 저장
```

### 2단계: 문항 생성 파이프라인
```
1. 지문 확인
   └─ 지문이 없으면 → PASSAGE_MASTER 프롬프트로 LLM 호출 → 지문 자동 생성

2. 프롬프트 구성
   └─ MASTER_PROMPT + 문항별 프롬프트(예: RC29) + 지문 + 난이도 → 최종 프롬프트

3. LLM 호출
   └─ Gemini API 또는 OpenAI API로 문항 생성 요청

4. JSON 파싱
   └─ 응답에서 JSON 추출 → 정규화 처리

5. 검증
   ├─ 공통 검증: 필수 필드, 선지 5개, 정답 범위(1-5)
   └─ 유형별 검증:
      ├─ RC29(어법): 밑줄 5개 위치 확인
      ├─ RC31-33(빈칸): 빈칸 위치 및 개수 확인
      └─ RC25(도표): 차트 데이터 일치 확인

6. 결과 저장
   ├─ 성공 → item_json + item_output 테이블 저장
   └─ 실패 → 검증 로그 기록 → 재시도 (최대 3회)
```

### 3단계: 결과 확인
```
item_output 테이블 → 최종 문항 조회 API → 화면 표시
```

---

## 📱 페이지별 사용 가이드

### 📊 대시보드 (`/`)
시스템 현황을 한눈에 파악할 수 있는 메인 화면

**표시 정보:**
- 서버 상태 (온라인/오프라인)
- 문항 요청 통계 (전체/대기/성공/실패)
- 로그 통계 (전체/최근 24시간/에러)
- 최근 요청 목록 (5개)
- 빠른 작업 버튼

---

### ➕ 새 문항 생성 (`/items/create`)
새로운 수능 문항을 생성하는 페이지

**입력 항목:**

| 필드 | 필수 | 설명 |
|------|------|------|
| 문항 유형 | ✅ | RC18~RC40 중 선택 |
| 난이도 | ✅ | 하/중하/중/중상/상 |
| 지문 | ❌ | 비워두면 AI가 자동 생성 |
| 주제/상황 | ❌ | 지문 생성 시 참고할 주제 |
| 추가 메모 | ❌ | 문항 생성 시 고려사항 |
| 차트 ID | ❌ | RC25(도표) 전용 |

**버튼 동작:**
- `🚀 생성 및 실행`: 요청 저장 후 즉시 문항 생성 시작
- `💾 저장만 하기`: 요청만 저장 (나중에 일괄 생성 가능)

**문항 유형 목록:**
```
RC18: 글의 목적     RC29: 어법         RC36: 글의 순서
RC19: 심경 변화     RC30: 지칭 추론    RC37: 글의 순서
RC20: 필자 주장     RC31: 빈칸(어구)   RC38: 문장 삽입
RC21: 함축 의미     RC32: 빈칸(어구)   RC39: 문장 삽입
RC22: 글의 요지     RC33: 빈칸(문장)   RC40: 요약문 완성
RC23: 글의 주제     RC34: 빈칸(문장)
RC24: 글의 제목     RC35: 무관한 문장
RC25: 도표 이해
RC26: 내용 일치(인물)
RC27: 내용 일치(안내문)
RC28: 어휘
```

---

### 📋 문항 요청 (`/items`)
생성된 문항 요청 목록을 관리하는 페이지

**상태 표시:**
- 🟡 `PENDING`: 대기 중 (아직 생성 안 됨)
- 🔵 `RUNNING`: 생성 중
- 🟢 `OK`: 생성 성공
- 🔴 `FAIL`: 생성 실패

**주요 기능:**
- 개별 문항 재생성
- 대기 중인 문항 일괄 생성
- 요청 삭제
- 생성 결과 상세 보기

---

### 📚 세트 문항 (`/sets`)
연계 문항 세트를 관리하는 페이지

**세트 유형:**
- LC16-17: 듣기 세트 (캠프 안내 방송 등)
- S41-42: 읽기 세트 (제목/요지)
- S43-45: 장문 읽기 세트 (요지/흐름/요약)

**세트 구성:**
- 세트 ID, 세트명
- 공통 지문
- 문항별 난이도 프로필

---

### 💬 프롬프트 관리 (`/prompts`)
문항 생성에 사용되는 프롬프트를 편집하는 페이지

**프롬프트 유형:**

| 키 | 설명 |
|---|---|
| `MASTER_PROMPT` | 🎯 모든 문항 생성에 적용되는 마스터 시스템 프롬프트 |
| `PASSAGE_MASTER` | 📄 지문 자동 생성 시 사용되는 마스터 프롬프트 |
| `숫자 (예: 29)` | 해당 문항 번호의 문항 생성 지침 |
| `P + 숫자 (예: P29)` | 해당 문항 번호의 지문 생성 지침 |

**편집 방법:**
1. 좌측 목록에서 프롬프트 선택
2. `✏️ 편집` 버튼 클릭
3. 내용 수정
4. `💾 저장` 버튼 클릭

---

### 📈 차트 데이터 (`/charts`)
RC25(도표 이해) 문항용 차트 데이터를 관리하는 페이지

**데이터 형식:**
```json
{
  "chart_id": "CH001",
  "chart_name": "Sample chart data",
  "raw_data_json": "{\"years\":[2020,2021,2022], \"sales\":[100,120,150]}"
}
```

---

### ⚙️ 설정 (`/config`)
시스템 설정을 관리하는 페이지

**주요 설정:**

| 키 | 기본값 | 설명 |
|---|---|---|
| `PROVIDER` | gemini | LLM 제공자 (gemini/openai) |
| `GEMINI_MODEL` | gemini-2.5-pro | Gemini 모델명 |
| `OPENAI_MODEL` | gpt-4.1-mini | OpenAI 모델명 |
| `TEMP_BASE` | 0.4 | 기본 Temperature |
| `TEMP_HIGH` | 0.7 | 높은 Temperature (창의적 생성용) |
| `MAX_RETRY` | 3 | 실패 시 재시도 횟수 |
| `TIMEOUT_MS` | 60000 | API 타임아웃 (밀리초) |
| `LOG_LEVEL` | INFO | 로그 레벨 |

---

### 📜 로그 (`/logs`)
시스템 로그를 조회하는 페이지

**로그 레벨:**
- `INFO`: 일반 정보
- `WARN`: 경고
- `ERROR`: 에러

---

## 🚀 앱 실행 방법

### 1. 서버 실행
```bash
cd D:\csat_mj_generator\web-app
npm run dev
```
→ http://localhost:3001 에서 API 서버 시작

### 2. 클라이언트 실행
```bash
cd D:\csat_mj_generator\web-app\client
npm start
```
→ http://localhost:3000 에서 React 앱 시작

### 3. 브라우저 접속
http://localhost:3000 으로 접속

---

## 🗄️ 데이터베이스 구조

### 테이블 목록

| 테이블 | 설명 | 주요 컬럼 |
|--------|------|----------|
| `config` | 시스템 설정 | key, value, description |
| `prompts` | 프롬프트 템플릿 | prompt_key, title, prompt_text, active |
| `item_requests` | 문항 생성 요청 | request_id, status, item_no, passage, level |
| `item_json` | 생성된 문항 JSON | request_id, raw_json, normalized_json, validation_result |
| `item_output` | 최종 출력 문항 | request_id, question, option_1~5, answer, explanation |
| `item_sets` | 세트 문항 정보 | set_id, set_name, common_passage, profile |
| `charts` | 차트 데이터 | chart_id, chart_name, raw_data_json |
| `logs` | 시스템 로그 | timestamp, level, tag, message |
| `errors` | 에러 로그 | timestamp, request_id, func_name, message |

---

## 🔧 환경 설정 (.env)

```env
# 서버 설정
PORT=3001
NODE_ENV=development

# API 키 설정
GEMINI_API_KEY=your_gemini_api_key
OPENAI_API_KEY=your_openai_api_key

# LLM 설정
PROVIDER=gemini
GEMINI_MODEL=gemini-2.5-pro
OPENAI_MODEL=gpt-4.1-mini
TEMP_BASE=0.4
MAX_RETRY=3

# 로그 레벨
LOG_LEVEL=INFO
```

---

## 📁 프로젝트 구조

```
D:\csat_mj_generator\
├── web-app/
│   ├── server/                 # 백엔드
│   │   ├── index.js           # 서버 엔트리포인트
│   │   ├── db/
│   │   │   └── database.js    # 데이터베이스 설정
│   │   ├── routes/            # API 라우트
│   │   │   ├── items.js
│   │   │   ├── prompts.js
│   │   │   ├── sets.js
│   │   │   ├── charts.js
│   │   │   ├── config.js
│   │   │   └── logs.js
│   │   ├── services/          # 비즈니스 로직
│   │   │   ├── itemPipeline.js    # 문항 생성 파이프라인
│   │   │   ├── llmClient.js       # LLM API 클라이언트
│   │   │   ├── promptBuilder.js   # 프롬프트 구성
│   │   │   ├── passageGenerator.js # 지문 생성
│   │   │   ├── jsonUtils.js       # JSON 파싱
│   │   │   └── validators/        # 검증 로직
│   │   └── scripts/           # 시드 스크립트
│   │       ├── seedPrompts.js
│   │       ├── seedItemRequests.js
│   │       └── ...
│   ├── client/                # 프론트엔드
│   │   └── src/
│   │       ├── App.js
│   │       ├── api/           # API 호출
│   │       └── pages/         # 페이지 컴포넌트
│   ├── data/
│   │   └── csat.db           # SQLite 데이터베이스
│   └── .env                  # 환경 설정
├── docs/
│   └── 사용자가이드.md
├── README.md
└── .gitignore
```

---

## 🔗 관련 링크

- **GitHub**: https://github.com/smilepat/csat_mj_generator
- **원본 시스템**: Google Apps Script + Google Sheets

---

## 📝 버전 정보

- **현재 버전**: 1.0.0
- **최종 업데이트**: 2024-12-30
- **개발 환경**: VS Code + Claude Code
