# 수능 문항 생성-검증 시스템 프로세스 가이드

## 1. 시스템 개요

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        수능 문항 생성-검증 시스템                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  Frontend (React)           │  Backend (Express)         │  Database (SQLite) │
│  - http://localhost:3000    │  - http://localhost:3001   │  - data/csat.db    │
│  - 9개 메뉴 페이지            │  - 7개 API 라우트           │  - 11개 테이블      │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 메뉴 구조 및 기능

| 메뉴 | 경로 | 주요 기능 | 연관 API |
|------|------|----------|----------|
| 📊 대시보드 | `/` | 전체 현황, 통계, 빠른 작업 | `/api/items`, `/api/logs` |
| 📋 문항 요청 | `/items` | 요청 목록, 상태 확인 | `/api/items/requests` |
| ➕ 새 문항 생성 | `/items/create` | 문항 생성 입력, 실행 | `/api/items/generate` |
| 📚 세트 문항 | `/sets` | 세트 관리 (41-42, 43-45) | `/api/sets` |
| 💬 프롬프트 관리 | `/prompts` | 프롬프트 조회/편집 | `/api/prompts` |
| 📈 차트 데이터 | `/charts` | RC25용 도표 관리 | `/api/charts` |
| ⚙️ 설정 | `/config` | LLM 설정 (모델, API키) | `/api/config` |
| 📜 로그 | `/logs` | 시스템 로그 조회 | `/api/logs` |
| 🎯 품질 대시보드 | `/quality` | 3겹 검증 결과, 메트릭스 | `/api/metrics` |

---

## 3. 핵심 프로세스 흐름

### 3.1 문항 생성 전체 흐름

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                           문항 생성 전체 프로세스                               │
└──────────────────────────────────────────────────────────────────────────────┘

[1] 프롬프트 관리 ──────────────────────────────────────────────────────────────
    │
    │  💬 프롬프트 관리 페이지에서:
    │  ├─ MASTER_PROMPT: 모든 문항의 기본 규칙 정의
    │  ├─ LC01~LC17: 듣기 문항별 세부 지침
    │  └─ RC18~RC45: 독해 문항별 세부 지침
    │
    ▼
[2] 문항 생성 요청 ─────────────────────────────────────────────────────────────
    │
    │  ➕ 새 문항 생성 페이지에서:
    │  ├─ 문항 유형 선택 (LC01~LC17, RC18~RC45)
    │  ├─ 난이도 선택 (하/중하/중/중상/상)
    │  ├─ 지문 입력 (선택) - 비우면 AI가 자동 생성
    │  └─ 주제/추가 메모 입력 (선택)
    │
    │  [프롬프트 미리보기] 버튼 클릭 시:
    │  └─ 검증 결과 + 프롬프트 미리보기 표시
    │
    ▼
[3] 파이프라인 실행 ────────────────────────────────────────────────────────────
    │
    │  generateItemPipeline() 실행:
    │  ┌─────────────────────────────────────────────────────────┐
    │  │ Step 0: 지문 필요 시 자동 생성                            │
    │  │   └─ passageGenerator.js → LLM 호출                     │
    │  │                                                         │
    │  │ Step 1: 프롬프트 구성                                     │
    │  │   └─ promptBuilder.js                                   │
    │  │   └─ MASTER_PROMPT + ITEM_PROMPT + CONTEXT 조합          │
    │  │                                                         │
    │  │ Step 2: LLM 호출                                         │
    │  │   └─ llmClient.js → Gemini/OpenAI API                   │
    │  │                                                         │
    │  │ Step 3: JSON 파싱                                        │
    │  │   └─ jsonUtils.js → parseItemJson()                     │
    │  │                                                         │
    │  │ Step 4: 정규화                                           │
    │  │   └─ jsonUtils.js → normalizeItemJson()                 │
    │  │   └─ correct_answer → answer 매핑                        │
    │  │   └─ stimulus → passage 매핑                             │
    │  │                                                         │
    │  │ Step 5: 공통 검증                                         │
    │  │   └─ validators/common.js → validateCommon()            │
    │  │   └─ question, options(5개), answer(1~5) 확인            │
    │  │                                                         │
    │  │ Step 6: 유형별 검증                                       │
    │  │   ├─ RC29: validators/grammar.js                        │
    │  │   ├─ RC31-33: validators/gap.js                         │
    │  │   └─ RC25: validators/chart.js                          │
    │  │                                                         │
    │  │ 실패 시: 최대 3회 재시도                                   │
    │  └─────────────────────────────────────────────────────────┘
    │
    ▼
[4] 결과 저장 ──────────────────────────────────────────────────────────────────
    │
    │  saveItemResults() 실행:
    │  ├─ item_json 테이블: 원본/정규화 JSON, 검증 로그
    │  ├─ item_output 테이블: 최종 문항 (question, options, answer)
    │  └─ item_metrics 테이블: 3겹 검증 점수
    │
    ▼
[5] 품질 평가 (3겹 검증) ───────────────────────────────────────────────────────
    │
    │  metricsService.js → saveItemMetrics():
    │  ├─ Layer 1 (구조 검증): question/options/answer 존재 여부
    │  ├─ Layer 2 (내용 품질): 선택지 중복, 정답 포함 여부
    │  └─ Layer 3 (수능 적합성): 지문 길이, 문장 수, 형식
    │
    │  종합 점수 → 등급(A~F) → 분류(APPROVE/REVIEW/REJECT)
    │
    ▼
[6] 결과 확인 ──────────────────────────────────────────────────────────────────
    │
    ├─ 📋 문항 요청: 상태(OK/FAIL), 상세 결과 확인
    ├─ 🎯 품질 대시보드: 등급별, 분류별 통계
    └─ 📊 대시보드: 전체 현황 요약
```

---

## 4. 메뉴간 상호관계

### 4.1 데이터 흐름 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                             메뉴 간 데이터 흐름                               │
└─────────────────────────────────────────────────────────────────────────────┘

                        ⚙️ 설정
                           │
                           │ LLM 모델/API키 설정
                           ▼
    💬 프롬프트 관리 ───────────────────────────────┐
           │                                        │
           │ 프롬프트 템플릿                          │
           ▼                                        │
    ➕ 새 문항 생성 ◄─── 📈 차트 데이터              │
           │              (RC25 도표 문항)           │
           │                                        │
           │ 생성 요청                               │
           ▼                                        │
    ┌──────────────────┐                           │
    │  파이프라인 실행   │◄──────────────────────────┘
    │  (백엔드 처리)     │
    └────────┬─────────┘
             │
             │ 결과 저장
             ▼
    ┌────────────────────────────────────────────────┐
    │                  DATABASE                       │
    │  ├─ item_requests (요청 상태)                    │
    │  ├─ item_json (원본/정규화 JSON)                 │
    │  ├─ item_output (최종 문항)                      │
    │  └─ item_metrics (품질 점수)                     │
    └─────────────────────┬──────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          │               │               │
          ▼               ▼               ▼
    📋 문항 요청    🎯 품질 대시보드   📊 대시보드
    (상세 결과)     (메트릭스 통계)    (전체 현황)
```

### 4.2 각 메뉴에서의 작업 → 영향 받는 다른 메뉴

| 작업 위치 | 수행 작업 | 영향 받는 메뉴 |
|-----------|----------|---------------|
| **⚙️ 설정** | LLM 제공자/모델 변경 | ➕ 새 문항 생성 (생성 품질 변경) |
| **⚙️ 설정** | API 키 설정 | ➕ 새 문항 생성 (API 호출 성공/실패) |
| **💬 프롬프트 관리** | MASTER_PROMPT 수정 | ➕ 새 문항 생성 (모든 문항 생성에 영향) |
| **💬 프롬프트 관리** | 특정 유형 프롬프트 수정 | ➕ 새 문항 생성 (해당 유형 문항에 영향) |
| **📈 차트 데이터** | 새 차트 등록 | ➕ 새 문항 생성 (RC25 문항에서 사용 가능) |
| **➕ 새 문항 생성** | 문항 생성 실행 | 📋 문항 요청 (새 요청 추가) |
| **➕ 새 문항 생성** | 문항 생성 실행 | 🎯 품질 대시보드 (메트릭스 추가) |
| **➕ 새 문항 생성** | 문항 생성 실행 | 📊 대시보드 (통계 업데이트) |
| **📚 세트 문항** | 세트 생성 | ➕ 새 문항 생성 (세트 선택 가능) |
| **📚 세트 문항** | 세트 실행 | 📋 문항 요청 (다중 요청 추가) |

---

## 5. 프로세스 단계별 상세 설명

### 5.1 문항 생성 입력 (새 문항 생성 페이지)

```
┌─────────────────────────────────────────────────────────────────┐
│  ➕ 새 문항 생성 입력 화면                                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  문항 유형: [LC01~LC17 / RC18~RC45 선택] ─────────────────────   │
│  난이도:   [하 / 중하 / 중 / 중상 / 상] ─────────────────────   │
│  지문:     [텍스트 입력 또는 비워두기] ───────────────────────   │
│  주제:     [선택 입력] ───────────────────────────────────────   │
│  추가메모: [선택 입력] ───────────────────────────────────────   │
│                                                                 │
│  [🔍 프롬프트 미리보기]  [🚀 바로 생성]  [💾 저장만 하기]          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

입력값 처리:
├─ 지문 비어있음 → AI가 자동 생성 (passageGenerator.js)
├─ 지문 입력됨 → 그대로 사용
├─ chart_id 입력 (RC25) → 차트 데이터 함께 전달
└─ 난이도/주제/메모 → 프롬프트 컨텍스트에 추가
```

### 5.2 프롬프트 구성 (promptBuilder.js)

```
┌─────────────────────────────────────────────────────────────────┐
│  프롬프트 구성 과정                                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [SYSTEM PROMPT]                                                │
│  └─ MASTER_PROMPT (DB에서 로드)                                  │
│     - JSON 출력 스키마                                           │
│     - 문항 생성 기본 원칙                                         │
│     - 저작권/독창성 규칙                                          │
│                                                                 │
│  [USER PROMPT]                                                  │
│  └─ ITEM_PROMPT (문항 번호별 DB에서 로드)                         │
│     + CONTEXT (사용자 입력 기반)                                  │
│       ├─ 지문 (있으면 PASSAGE_GIVEN, 없으면 생성 지시)            │
│       ├─ 난이도 정보                                             │
│       ├─ 추가 메모                                               │
│       ├─ 도표 데이터 (RC25)                                      │
│       └─ 세트 정보 (세트 문항)                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.3 JSON 정규화 (jsonUtils.js)

```
┌─────────────────────────────────────────────────────────────────┐
│  LLM 응답 → 정규화 매핑                                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  LLM 출력 필드          →    시스템 내부 필드                     │
│  ──────────────────────────────────────────────────             │
│  correct_answer (정수)  →    answer (문자열 "1"~"5")             │
│  question_stem         →    question                            │
│  stimulus              →    passage                              │
│  transcript            →    passage                              │
│  options (배열)        →    options (정확히 5개로 정규화)          │
│                                                                 │
│  추가 보존 필드:                                                  │
│  - vocabulary_difficulty                                         │
│  - low_frequency_words                                           │
│  - grammar_meta (RC29)                                           │
│  - gapped_passage (RC31-33)                                      │
│  - chart_data (LC10)                                             │
│  - image_prompt (LC04)                                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.4 3겹 검증 시스템 (metricsService.js)

```
┌─────────────────────────────────────────────────────────────────┐
│  3겹 검증 시스템 (품질 평가)                                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Layer 1: 구조 검증 (기계적)                                      │
│  ├─ question 존재 여부                                           │
│  ├─ options 배열 (정확히 5개)                                    │
│  ├─ answer 존재 (1~5 범위)                                       │
│  └─ 점수: 0-100점                                                │
│                                                                 │
│  Layer 2: 내용 품질 (규칙 + AI)                                   │
│  ├─ 정답이 선택지에 포함되어 있는지                                │
│  ├─ 선택지 간 중복 여부                                           │
│  └─ 점수: 0-100점                                                │
│                                                                 │
│  Layer 3: 수능 적합성 (규칙 기반)                                  │
│  ├─ 지문 길이 (단어 수)                                           │
│  ├─ 문장 수                                                      │
│  ├─ 평균 문장 길이                                               │
│  └─ 점수: 0-100점                                                │
│                                                                 │
│  종합 점수 계산:                                                  │
│  final_score = (L1 * 0.3) + (L2 * 0.4) + (L3 * 0.3)              │
│                                                                 │
│  등급 및 분류:                                                    │
│  ├─ A등급 (90+): APPROVE (승인)                                  │
│  ├─ B등급 (80-89): APPROVE (승인)                                │
│  ├─ C등급 (70-79): REVIEW (검토)                                 │
│  ├─ D등급 (60-69): REVIEW (검토)                                 │
│  └─ F등급 (60미만): REJECT (반려)                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 6. 데이터베이스 테이블 관계

```
┌─────────────────────────────────────────────────────────────────┐
│                    테이블 관계도                                  │
├─────────────────────────────────────────────────────────────────┤

┌──────────────┐     1:N     ┌──────────────┐
│   prompts    │◄────────────│item_requests │
│  (프롬프트)   │             │  (요청)       │
└──────────────┘             └──────┬───────┘
       │                            │
       │ 1:1                        │ 1:N
       ▼                            ▼
┌──────────────┐             ┌──────────────┐
│prompt_metrics│             │  item_json   │
│(프롬프트품질) │             │ (JSON결과)   │
└──────────────┘             └──────────────┘
                                    │
                                    │ 1:1
                                    ▼
                             ┌──────────────┐
                             │ item_output  │
                             │ (최종문항)    │
                             └──────────────┘
                                    │
                                    │ 1:1
                                    ▼
                             ┌──────────────┐
                             │ item_metrics │
                             │ (품질메트릭)  │
                             └──────────────┘

┌──────────────┐     1:N     ┌──────────────┐
│  item_sets   │◄────────────│item_requests │
│   (세트)      │             │ (세트문항)    │
└──────────────┘             └──────────────┘

┌──────────────┐
│   charts     │ ──► RC25 문항에서 참조
│  (도표)       │
└──────────────┘

┌──────────────┐
│   config     │ ──► LLM 설정값 저장
│   (설정)      │
└──────────────┘

┌──────────────┐
│    logs      │ ──► 시스템 로그
│   (로그)      │
└──────────────┘
```

---

## 7. API 엔드포인트 정리

### 7.1 문항 관련 (/api/items)

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/items/requests` | 요청 목록 조회 |
| GET | `/api/items/requests/:id` | 특정 요청 상세 조회 |
| POST | `/api/items/requests` | 새 요청 생성 |
| POST | `/api/items/generate/:id` | 문항 생성 실행 |
| POST | `/api/items/generate-pending` | 대기 중인 요청 일괄 처리 |
| POST | `/api/items/preview-prompt` | 프롬프트 미리보기 |
| DELETE | `/api/items/requests/:id` | 요청 삭제 |
| GET | `/api/items/outputs` | 생성된 문항 결과 조회 |

### 7.2 프롬프트 관련 (/api/prompts)

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/prompts` | 프롬프트 목록 조회 |
| GET | `/api/prompts/:key` | 특정 프롬프트 조회 |
| POST | `/api/prompts` | 새 프롬프트 생성 |
| PUT | `/api/prompts/:key` | 프롬프트 수정 |
| DELETE | `/api/prompts/:key` | 프롬프트 삭제 |
| POST | `/api/prompts/:key/evaluate` | 프롬프트 품질 평가 |

### 7.3 메트릭스 관련 (/api/metrics)

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/metrics` | 메트릭스 목록 조회 |
| GET | `/api/metrics/summary` | 요약 통계 |
| GET | `/api/metrics/:requestId` | 특정 요청의 메트릭스 |

---

## 8. 사용 시나리오

### 시나리오 1: 첫 문항 생성

```
1. ⚙️ 설정 → Gemini API 키 확인/설정
2. 💬 프롬프트 관리 → MASTER_PROMPT 확인
3. ➕ 새 문항 생성 → RC29 선택, 난이도 중, 지문 비우기
4. [🔍 프롬프트 미리보기] → 검증 통과 확인
5. [🚀 바로 생성] → 생성 대기
6. 📋 문항 요청 → 결과 확인 (OK/FAIL)
7. 🎯 품질 대시보드 → 등급/점수 확인
```

### 시나리오 2: 프롬프트 수정 후 재생성

```
1. 🎯 품질 대시보드 → 낮은 등급 문항 확인
2. 💬 프롬프트 관리 → 해당 유형 프롬프트 수정
3. ➕ 새 문항 생성 → 동일 조건으로 재생성
4. 📋 문항 요청 → 개선 여부 비교
```

### 시나리오 3: 세트 문항 생성 (RC43-45)

```
1. 📚 세트 문항 → 새 세트 생성 (RC43-45)
2. 세트 정보 입력 (공통 지문, 프로필)
3. [세트 생성 실행] → 3개 문항 동시 생성
4. 📋 문항 요청 → 세트 문항들 결과 확인
```

---

## 9. 문제 해결 가이드

| 증상 | 원인 | 해결 방법 |
|------|------|----------|
| "answer 필드가 없습니다" | LLM이 correct_answer로 출력 | jsonUtils.js에서 자동 매핑됨 (수정 완료) |
| "GEMINI_API_KEY가 설정되지 않았습니다" | .env 파일 없음 | .env 파일에 API키 설정 |
| 문항 생성 실패 반복 | 프롬프트 문제 | 💬 프롬프트 관리에서 해당 유형 프롬프트 확인 |
| 낮은 품질 점수 | 지문/선택지 품질 | 프롬프트 개선 또는 수동 지문 입력 |
